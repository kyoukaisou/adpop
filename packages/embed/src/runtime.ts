/*
  ADPOP の本体 —— **発火が決まってから取りに行く側**(要件書 §4-1 の2段構え)。
  MIT(このディレクトリのみ)。

  🔴 PR1 では中身を入れない。PR2 で Shadow DOM の描画とイベント送信が入る。
  ⚠ 出ないまま終わる訪問者にはこのファイルを1バイトも配らない、が2段構えの目的。
*/
export const ADPOP_RUNTIME_VERSION = "0.0.0";
